export enum AUTH_SERVICES_NAMES {
    REGISTER_USER = 'auth.register.user',
    LOGIN_USER = 'auth.login.user',
    VERIFY_TOKEN = 'auth.verify.user',
    TOKEN_PASSWORD = 'auth.get.password',
    RECOVER_PASSWORD = 'auth.recover.password'   
}
